import { AutoConfigDirective } from './auto-config.directive';

describe('AutoConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoConfigDirective();
    expect(directive).toBeTruthy();
  });
});
